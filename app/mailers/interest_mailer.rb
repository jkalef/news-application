class InterestMailer < ApplicationMailer

  #every time a post is created with a specific tag, I want to notify the user who
  #has that interest that a new post was made.
  def send_interest_post(tag)

  end

end
