    <div id="<%= dom_id(user) %>">
          <%= link_to "Delete User", admin_user_path(user), remote: true, method: :delete, class: "btn btn-danger", data: {confirm: "Are you sure you want to delete this user?"} %>
    </div>