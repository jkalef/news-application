<h1>Here is your post!</h1>

<%# display the post using my instance variable from the posts_controller %>
<h3><%= @post.title %></h3>
<p><%= @post.body %></p>

<%# Add a link to edit your post %>
<%= link_to "Edit", edit_post_path(@post) %>
<%= button_to "Delete", post_path(@post), method: :delete, data: {confirm: "Are you sure you want to delete?"} %>